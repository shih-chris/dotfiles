# This repo's structure
This repo store my dotfiles. They are all symlinked to their respective config locations (i.e. ~/.config/opencode). Make updates in this repo rather than in the config locations themselves

ALWAYS update the config files in this repo, rather than trying to update the ones in ~/.config. You can assume that they are always symlinked
